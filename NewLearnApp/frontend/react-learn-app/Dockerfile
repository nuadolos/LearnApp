# Использование чужого образа для создание своего
FROM node

# Папка расположения проекта
WORKDIR /app

COPY package.json .

# Команда, используемая при сборке образа
RUN npm install

# Вместо /app можно использовать . (Так как указана папка расположения)
COPY . /app 

# Используемый порт
EXPOSE 3000

CMD [ "npm", "start" ]